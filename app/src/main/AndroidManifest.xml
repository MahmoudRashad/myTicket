<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myticket">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_INTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".View.Activity.EditDetail" />
        <activity android:name=".View.Activity.EditAccount" >

        </activity>
        <activity android:name=".View.Activity.CinemaDetailsPage"
            android:theme="@style/AppTheme"
            android:parentActivityName=".View.Activity.HomeCinema"
            >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".View.Activity.HomeCinema" />
        </activity>

        <activity android:name=".View.Activity.AnyResultsPage" />
        <activity android:name=".View.Activity.SearchPage" />
        <activity android:name=".View.Activity.ResetPassword" />
        <activity android:name=".View.Activity.MovieDetailsPage"
            android:theme="@style/AppTheme"
            android:parentActivityName=".View.Activity.HomeCinema"
            >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".View.Activity.HomeCinema" />
        </activity>

        <activity android:name=".View.Activity.Login"/>


        <activity android:name=".View.Activity.Gate" />
        <activity android:name=".View.Activity.MainActivity" />
        <activity android:name=".View.Activity.HomeCinema" />
        <activity android:name=".View.Activity.ForgetPassword" />
        <activity
            android:name=".View.Activity.Register"
            android:windowSoftInputMode="adjustResize" />

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".View.Activity.MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".View.Activity.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>